import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";

const Header = () => {
  return (
    <header>
      <FontAwesomeIcon className="logo-list" icon="fa-solid fa-list-ul" />
      <h1>To Do List</h1>
    </header>
  );
};

export default Header;
